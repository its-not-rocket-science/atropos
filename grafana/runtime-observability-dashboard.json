{
  "title": "Atropos Runtime Observability",
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "10s",
  "panels": [
    {
      "title": "API Error Rate",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(atropos_api_errors_total[5m])) by (path)",
          "legendFormat": "{{path}}"
        }
      ]
    },
    {
      "title": "Queue Depth by Env",
      "type": "timeseries",
      "targets": [
        {
          "expr": "atropos_runtime_queue_depth",
          "legendFormat": "{{env}}"
        }
      ]
    },
    {
      "title": "API Latency P95",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(atropos_api_request_latency_seconds_bucket[5m])) by (le, path))",
          "legendFormat": "{{path}}"
        }
      ]
    },
    {
      "title": "Duplicate Ingestion Rate",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(atropos_duplicate_ingestion_rejections_total[5m])) by (env)",
          "legendFormat": "{{env}}"
        }
      ]
    },
    {
      "title": "Noisiest Environments",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(atropos_api_requests_by_env_total[5m])) by (env)",
          "legendFormat": "{{env}}"
        }
      ]
    },
    {
      "title": "Worker Dependency Failures",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(atropos_worker_dependency_failures_total[5m])) by (dependency)",
          "legendFormat": "{{dependency}}"
        }
      ]
    }
  ]
}
